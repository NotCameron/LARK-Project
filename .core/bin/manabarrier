#!/bin/bash

if [ $# -gt 1 ] || [ $# -lt 1 ];then
 echo "You need to enter one passphrase"
 exit
fi

if [ $PWD/manabarrier ]; then

 if [ $1 == jazz ]; then
  touch sombrero
 fi

else
 echo "A ghostly voice says to you that you have entered the wrong pass phrase"
fi
